kind: Deployment
apiVersion: apps/v1
metadata:
  name: fe-admin
  labels:
    app: fe-admin

spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-admin
  template:
    metadata:
      labels:
        app: fe-admin
    spec:
      containers:
        - name: fe-admin
          image: fe-admin
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: fe-admin-env
          ports:
            - name: web
              containerPort: 3000
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "50m"
      imagePullSecrets:
        - name: selcloud
